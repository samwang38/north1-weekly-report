# 「月進度」分頁的月目標怎麼來的

週報裡的「月進度」分頁會顯示各店的**月目標、達成率、3PP／SA Care 目標率**。
這些數字不是從 EPB 來的，是上網去讀線上的 Google 目標表。

要讀那張表需要一個**通行證檔案**（Google 服務帳號金鑰）。

---

## 沒有通行證會怎樣？

**週報照常產出，不會壞掉。** 只是「月進度」分頁的這幾欄會空白：

| 欄位 | 沒通行證時 |
|---|---|
| 3PP 搭售率、SA Care 搭售率、EDU 佔比 | ✅ 正常（這些來自 EPB） |
| 月目標、達成率、3PP 目標率、SA Care 目標率 | ❌ 空白 |

分頁標題會出現「⚠️ 本月目標未取得」提醒你。

---

## 怎麼裝通行證

### 1. 跟 Bob 要檔案

檔名是 `forecast-service-account.json`。

⚠️ **這個檔案不會放在 GitHub 上，也不要放。** 它能存取公司所有目標表，
等同密碼。請 Bob 用 AirDrop 或其他私下管道傳給你，**不要用群組聊天室、
不要 email、不要放共用雲端資料夾**。

### 2. 雙擊「安裝目標表金鑰.command」

就在週報資料夾裡，跟啟動檔放在一起。

**不用開終端機、不用自己建資料夾、不用打任何指令。**
（`~/.config/studioa` 這個資料夾預設本來就不存在，這支工具會自動建。）

它會做這些事：

1. 建好資料夾
2. 自動到「下載」「桌面」「文件」找 `forecast-service-account.json`
   - 找到 → 問你要不要安裝，按 `y` 就搬好，並把權限設成只有你能讀
   - 找不到 → 幫你打開該放的資料夾，把檔案拖進去，再雙擊這支工具一次
3. 檢查檔案是不是有效的金鑰、需要的套件在不在，結果直接印在畫面上

看到「✅ 完成！」就好了。

### 3. 確認有沒有成功

重新雙擊「啟動…週報.command」。啟動訊息裡：

- 有看到 `[提醒] 找不到目標表金鑰` → 沒裝成功，再跑一次第 2 步
- 沒有那行提醒 → 成功，跑出來的月進度就會有目標與達成率

---

## 另一個提醒：套件

「月進度」讀目標還需要兩個 Python 套件（`gspread`、`google-auth`）。
啟動腳本會自動裝，裝不起來時會印出可以直接複製的指令。

如果看到 `[提醒] 缺 gspread / google-auth`，照畫面上那行指令跑一次就好。

---

## 常見狀況

**Q：啟動時說 `externally-managed-environment`**
你的 Mac 裝了 Homebrew 版的 Python。啟動腳本會自動改用其他方式重試；
若仍失敗，畫面會印出一行 `--break-system-packages` 的指令，複製執行即可。

**Q：`pip3 install` 明明成功了，程式還是說找不到套件**
`pip3` 和 `python3` 可能是**不同版本的 Python**。這是最常見的坑。
不要用 `pip3`，改用啟動腳本印出來的那行（形式是 `"某個python路徑" -m pip install ...`），
它會把套件裝到真正在跑程式的那個 Python 裡。

**Q：通行證會過期嗎**
不會自動過期。若 Google 那邊撤銷了金鑰，月目標會變空白並在啟動訊息提醒，
屆時跟 Bob 要新的即可。
